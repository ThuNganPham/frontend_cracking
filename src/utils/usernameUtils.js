// const generateUsername = (email)=> {
//   const baseUsername = email.split('@')[0]; // Lấy phần trước dấu '@'
//   const uniqueSuffix = Math.floor(1000 + Math.random() * 9000); // Tạo số ngẫu nhiên 4 chữ số
//   return `${baseUsername}_${uniqueSuffix}`; // Kết hợp để đảm bảo tính duy nhất
// };

// module.exports = generateUsername;